.DS_Store
node_modules/
dist/
*.js.map
.env
.env.*
!.env.example
!.env.harness.example
!.env.*.example
.plan/
# Prevent accidentally committing agent.yaml from agent-runtime
# (it overrides AGENT_CONFIG_B64 and breaks cloud deploys)
packages/agent-runtime/agent.yaml
packages/agent-runtime/agent.yml
packages/agent-runtime/agent.harness.yaml
# Local copy from docs/examples (see harness-tutorial.md)
agent.sandbox.yaml

# CocoIndex Code (ccc) — local semantic index
.cocoindex_code/

# CodeGraph — local AST graph (*.db ignored inside .codegraph/.gitignore)
.codegraph/

# Cursor — local IDE config (not shared)
.cursor/
